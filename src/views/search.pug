extends base
include mixins/video

block content
    form(method="GET")
        input(name="keyword" placeholder="Search by title", required)
        input(value="Search now",type="submit")
    div
        each video in videos
            +video(video)
        else 
            h1 Not Found Videos